# mppi_core

Use `MPPI.md` as the main design and runtime-contract document for this
package.

The disturbance-aware robust grasp policy MVP is documented in
`docs/robust_grasp_policy.md` and configured by `task/robust_grasp_hold.yaml`.
